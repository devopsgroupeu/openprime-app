apiVersion: v1
kind: Service
metadata:
  name: {{ include "openprime-app.fullname" . }}
  labels:
    {{- include "openprime-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "openprime-app.selectorLabels" . | nindent 4 }}
